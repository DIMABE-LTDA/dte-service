name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      # En Linux las deps nativas de firma del motor son paquetes del sistema.
      # Pango: WeasyPrint genera los PDF de las muestras impresas (y su test).
      - name: Instalar libs nativas (xmlsec/libxml2, Pango)
        run: sudo apt-get update && sudo apt-get install -y libxml2-dev libxmlsec1-dev pkg-config libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz-subset0 fonts-liberation

      # El motor (cl_dte_lib) es privado: el extra `engine` lo instala desde
      # el tag fijado en pyproject. ENGINE_TOKEN = PAT fine-grained / deploy key con lectura a
      # cl_dte_lib (el github.token por defecto no alcanza otro repo privado).
      - name: Auth git para el repo privado del motor
        run: git config --global url."https://x-access-token:${{ secrets.ENGINE_TOKEN }}@github.com/".insteadOf "https://github.com/"

      - name: Instalar servicio + motor (pineado al tag)
        run: pip install -e ".[dev,engine]"

      # Docker construye con el motor vendorizado, no con el instalado: si esa
      # copia no es el tag fijado, se despliega algo que no se probó.
      - name: El motor vendorizado es el tag fijado
        run: python scripts/check_vendor.py

      - name: Lint (ruff)
        run: ruff check .

      - name: Formato (ruff)
        run: ruff format --check .

      - name: Tipos (mypy)
        run: mypy

      - name: Tests (pytest)
        run: pytest -q

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Lint (eslint)
        run: npm run lint
      - name: Formato (prettier)
        run: npm run format:check
      - name: Tests (vitest)
        run: npm run test:run
      - name: Build (tsc + vite)
        run: npm run build
